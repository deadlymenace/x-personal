version: "3.8"

services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - bookmark-data:/app/server/data
    restart: unless-stopped

volumes:
  bookmark-data:
